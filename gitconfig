[diff]
	external = ~/dotfiles/git-meld.sh
[merge]
    tool = mymeld
    conflictstyle = diff3
[mergetool "mymeld"]
    cmd = meld "$LOCAL" "$MERGED" "$REMOTE"
[alias]
	diffall = git-diffall
[core]
	editor = gvim -f
